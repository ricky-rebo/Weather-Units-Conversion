import { c2f, celsiusToFahrenheit, f2c, fahrenheitToCelsius } from "./temp_units";

export const Greeter = (name: string) => `Hello ${name}`;


export {
  c2f, celsiusToFahrenheit,
  f2c, fahrenheitToCelsius
}